import React from 'react';

const ContainerProfile = ({ children }: any) => {
    return(
        <div className="profile-container">
            {children}
        </div>
    )
}

export default ContainerProfile